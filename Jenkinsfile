pipeline
{
  agent any
    stages {
        stage("stage1") {
          steps {
              script {
                   type 'fileA'
                   //def data = readFile(file: 'fileA.txt')
                   //println(data)
               }            //sh 'cat /workflow/sample/fileA'
          }
        }
    }
}
