pipeline
{
  agent any
    stages {
        stage("stage1") {
          steps {
              script {
                   sh 'ls'
                   //def data = readFile(file: 'fileA.txt')
                   //println(data)
               }            //sh 'cat /workflow/sample/fileA'
          }
        }
    }
}
